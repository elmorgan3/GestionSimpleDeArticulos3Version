<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.morga.gestionsimpledearticulos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaCompletaActivity" />
        <activity android:name=".ListaFiltradaActivity" />
        <activity android:name=".CrearActivity" />
        <activity
            android:name=".ListaConIconActivity"
            android:label="@string/title_activity_lista_con_icon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListaMovimientosActivity" />
        <activity android:name=".EntradaOSalidaActivity"></activity>
    </application>

</manifest>